{
  "name": "github-issue-automation",
  "version": "1.0.0",
  "description": "Automated GitHub issue management with AWS Bedrock",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "test:local": "npm run build && node dist/test/test-local.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.1027.0",
    "@octokit/rest": "^20.0.2"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3",
    "jest": "^29.7.0",
    "@types/jest": "^29.5.11",
    "ts-jest": "^29.1.1"
  }
}
